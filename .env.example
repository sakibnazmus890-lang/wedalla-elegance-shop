# Supabase settings are only needed if Supabase-backed features are enabled.
VITE_SUPABASE_URL=

# Supabase settings are only needed if Supabase-backed features are enabled.
VITE_SUPABASE_PUBLISHABLE_KEY=
